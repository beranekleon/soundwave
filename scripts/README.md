# scripts

Development and release helper scripts.

- `launch.bat`: starts backend and desktop dev app in separate terminals.
- `dev/start-backend.bat`: internal backend bootstrap (venv activation + uvicorn).
